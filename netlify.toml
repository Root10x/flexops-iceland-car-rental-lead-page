[build]
  command = ""          # No build needed for plain HTML
  publish = "."         # Publish the root folder

# Optional: Redirect /admin to the admin folder
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
